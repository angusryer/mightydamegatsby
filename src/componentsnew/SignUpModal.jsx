import React from 'react'
import styled from 'styled-components';
import * as AuthLogic from '../utilities/authLogic'; 
import { Auth } from 'aws-amplify';

export default function SignIn() {
    return (
        <div>
            
        </div>
    )
}
